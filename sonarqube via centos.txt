run  the centos AMI
install docker on the AMI
install thee plugin 
check docker status
systemctl status docker
systemctl start docker

now 
pull docker image by command
# docker image pull sonarqube:latest

check docker image by 
# docker images

#docker container run -d --name sonarqube -p 9000:9000 sonarqube:latest
#docker ps 

#yum install git
cd into home and then cd into centos  
#git clone https://github.com/up1/maven_java_web_example

cd into opt 
install apache maven 
# wget http://apachemirror.wuchna.com/maven/maven-3/3.6.3/binaries/apache-maven-3.6.3-bin.tar.gz
# tar zxf apache-maven-3.6.3-bin.tar.gz

cd into the bin of maven-3.6.3

#yum install java-11-openjdk-devel
#./mvn -version

for all os be able to use maven use------------->this command is executed everytime you exit the terminal it allow the usage of mvn command
# export PATH=$PATH:/opt/apache-maven-3.6.3/bin/


go to the repository in /home/centos/...."repository name"

create/edit  a file sonar-project.properties
remember that the src file must be present in the repository paste the following data :-
-------------------------------------------------------------
#Required metadata
sonar.projectKey=java-sonar-runner-simple
sonar.projectName=Simple java project analyzed with the sonarqube runner
sonar.project Version=1.0
#Language
sonar.language=java
# Comma-seperated paths to directories with sources(required)
sonar.sources=src
#Encoding the source file
sonar.sourceEncoding=UTF-8
---------------------------------------------------------------
# mvn compile 

paste the project url from the sonarqube from web browser



